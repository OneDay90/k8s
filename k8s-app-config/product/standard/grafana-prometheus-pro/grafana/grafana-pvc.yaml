apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-metrics-grafana
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "local-storage"
  resources:
    requests:
      storage: 10Gi

